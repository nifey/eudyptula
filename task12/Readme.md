# Instructions

- Compile the module by running
  ```bash
  make
  ```

- Load the module
  ```bash
  sudo insmod list.ko
  ```

- Check dmesg log for output
  ```bash
  sudo dmesg
  ```

- Unload the module
  ```bash
  sudo rmmod list
  ```
